import type { AlertPreference } from "@/lib/types";

export const alertPreferenceLabels: Record<AlertPreference, string> = {
  early_setups: "Early Setups",
  risk_alerts: "Risk Alerts",
  balanced: "Balanced",
};
